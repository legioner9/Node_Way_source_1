const { _require } = require("st_require");

const tsh = _require ('tsh');
const Node = tsh.hpw.LinkedList._1_single_proto;

// Usage

const n1 = new Node(null, { name: 'first' });
const n2 = new Node(n1, { name: 'second' });
const n3 = new Node(n2, { name: 'third' });

console.dir(n1);
console.dir(n2);
console.dir(n3);
debugger;