const { s_rmdirSync } = require ( 'st_ini_fs' );
const Path = require ( 'path' );
debugger
const path_to_clear = Path.join ( __dirname, 'ROOT.dir' );
s_rmdirSync ( path_to_clear );

console.log(`clear dir = ${path_to_clear}`)

process.exit ( 0 );